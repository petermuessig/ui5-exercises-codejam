{
    "welcomeFile": "index.html",
    "authenticationMethod": "none",
    "routes": [
        {
            "source": "/browse(.*)",
            "destination": "browse-bookshop",
            "authenticationType": "none" 
       },
        {
            "source": "^(.*)$",
            "target": "$1",
            "authenticationType": "none",
            "localDir": "finished-webapp"
        }
    ]
}